{
  "name": "meu-portfolio",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:worker": "vite build && node scripts/preparar-shell.mjs",
    "preview": "vite preview",
    "verificar": "node scripts/snapshot.mjs --saida out && node scripts/dom-diff.mjs snapshot/pt.html out/pt.html --allow scripts/expected-diffs.pt.json && node scripts/dom-diff.mjs snapshot/en.html out/en.html --allow scripts/expected-diffs.en.json && node scripts/import-graph.mjs --config scripts/boundary.config.json && node scripts/medir-render.mjs",
    "migrations:validar": "node scripts/validar-migrations.mjs",
    "migrations:aplicar": "node scripts/validar-migrations.mjs --aplicar"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.3.1",
    "linkedom": "^0.18.13",
    "playwright": "^1.62.1",
    "pngjs": "^7.0.0",
    "vite": "^8.1.0"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.112.3",
    "lucide": "^1.21.0"
  }
}
